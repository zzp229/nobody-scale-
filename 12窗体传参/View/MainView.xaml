<Window x:Class="_12窗体传参.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_12窗体传参.View"
        xmlns:vm ="clr-namespace:_12窗体传参.ViewModel"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:MainViewModel}"
        Title="MainView" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBox Text="{Binding Txt}" FontSize="12" Width="200" Height="30" />
            <TextBlock Text="{Binding Txt}" Width="200" Height="30" />
            <Button Content="aaa" Command="{Binding aaCommand}"/>
        </StackPanel>
        
        

    </Grid>
</Window>
